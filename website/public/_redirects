# Netlify/Vercel Redirects pour SEO
# S'assurer que toutes les URLs importantes sont accessibles

# Redirects de trafic (SEO)
/shop /shop 200
/download /download 200
/about /about 200
/faq /faq 200

# Protection des pages privées
/dashboard /* /login 302
/verify-email /* /login 302

# Sitemap et robots
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200

# Fallback pour React Router
/* /index.html 200

